apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: pm-pg-cluster
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: pg-cluster
  podMetricsEndpoints:
  - port: metrics
